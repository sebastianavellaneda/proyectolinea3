<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FutbolTeamUp"
        tools:targetApi="31">

        <!-- Actividad principal -->
        <activity
            android:name=".interfaz.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otras actividades -->
        <activity android:name=".interfaz.MainActivity" />
        <activity android:name=".interfaz.RegistroActivity" />
        <activity android:name=".interfaz.CrearPartidoActivity" />
        <activity android:name=".interfaz.BuscarPartidoActivity" />
        <activity android:name=".interfaz.ChatActivity" />
        <activity android:name=".interfaz.EditarUsuarioActivity" />
        <activity android:name=".interfaz.GestionarPartidosActivity" />

    </application>

</manifest>
